#!/bin/sh

OUTPUT=/tmp/xctester
SOURCE_DIR="`dirname $0`"

SDK_PATH="`xcrun --show-sdk-path`"
FRAMEWORKS_PATH="$SDK_PATH/../../Library/Frameworks"

xcrun swiftc -o "$OUTPUT" -F$FRAMEWORKS_PATH -Xlinker -rpath \
	-Xlinker $FRAMEWORKS_PATH $SOURCE_DIR/../code/*.swift *.swift

"$OUTPUT"
rm -f "$OUTPUT"
